# Hyperframes Composition Brief: Local RAG Assistant — technique

## Objective
Create a technical 25-second portfolio video making the manually orchestrated RAG pipeline visible.

## Output
- Composition: `brag-output/local-rag-technical/composition/`
- Render: `brag-output/local-rag-technical/brag.mp4`
- Format: landscape, 1920x1080, 25 seconds

## Source material
- Project root: source project (local path intentionally omitted)
- Real UI: `assets/img/local-rag-demo.jpg`
- Real architecture: `assets/img/architecture.png`
- Real functions referenced: `extract_pages_from_pdf`, `chunk_text`, `retrieve_topk`, `format_context`
- Copy: « Pas de framework RAG clé en main. »; « Une réponse locale. Une source vérifiable. »

## Creative direction
Technical build log with monospace code cards, the real architecture and the real UI. State accurately that specialized libraries are used while orchestration is authored in Python.

## Visual identity
Background #17212b; text #f7f3e9; accents #f58220 and #b8e26f; Consolas/Inter fallbacks.

## Storyboard
1. Hook — 4.2s — claim and learning angle.
2. Ingestion — 4.8s — authentic extraction/chunking/index code.
3. Architecture — 5.5s — real Archify diagram.
4. Retrieval/generation — 5.0s — authentic retrieval/context/completion labels.
5. Proof — 5.5s — real UI, source and stack.

## Audio
Intentional silence. No external audio is included because the bundled track has no verified redistribution terms.

## Hyperframes requirements
Show real code concepts and UI, keep text readable, validate with `hyperframes lint`, `inspect`, `check`, `keyframes` and snapshots before rendering.
